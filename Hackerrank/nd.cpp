#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int solve(long long a[],int n)
{
	if(n==2)
		
}

int main() 
{
	int n,k;
	cin>>n>>k;
	long long a[n];
	for(int i=0; i<n; i++)
		cin>>a[i];
	vector<long long> v;
	solve(a,n);

    return 0;
}
